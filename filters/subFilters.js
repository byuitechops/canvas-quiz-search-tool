const contains = function () { };

const equalTo = function () { };

const greaterThan = function () { };

const lessThan = function () { };

const length = function () { };

const questionTypes = function () { };

module.exports = {
    contains,
    equal_to: equalTo,
    greater_than: greaterThan,
    less_than: lessThan,
    length,
    questionTypes

};